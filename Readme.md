Basic Todo List Web application without the local Storage but runs on server.

Even if you refresh the page, the contents won't be wiped out.But if we restart the server, the page contents will be lost.